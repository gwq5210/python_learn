#!/usr/bin/env python
# coding=utf-8

import os;
from Utils import *;

CopyToIps(presscall_ips_red, pwd = red_password);
CopyToIps(presscall_ips);
CopyToIps(lsvr_ips);
CopyToIps(d_access_ips, pwd = red_password);
CopyToIps(sr_access_ips, pwd = red_password);
CopyToIps(d_cache_ips, pwd = red_password);
CopyToIps(sr_cache_ips, pwd = red_password);
